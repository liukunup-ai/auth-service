Name: auth.http
Host: 0.0.0.0
Port: 7001  # 监听端口

Mysql:  # 请根据实际情况修改
  DataSource: "user:pass@tcp(mysql.homelab.lan:3306)/testing?charset=utf8mb4&parseTime=True&loc=Local"

Redis:
  Addrs: # 节点 (单机或哨兵时只需要填写一个地址)
    - docker.homelab.lan:6379
  DB: 0
  Password: ""

# JWT
Auth:
  AccessSecret: zvcbozvafjxcbdxh911bq101cblrgqbt  # 生产环境请自行更换
  AccessExpiresIn: 86400    # 设置 Access Token 过期时间 1 天
  RefreshSecret: X18lqTD9Vkpm0IHN6W6xKcwDGHgsyIQY  # 生产环境请自行更换
  RefreshExpiresIn: 604800  # 设置 Refresh Token 过期时间 7 天
  BlacklistCachePrefix: "auth:token:blacklist:"   # 设置 Token 缓存黑名单前缀 (用于轮换/登出等场景)

Captcha:
  Enable: true   # 是否启用
  ExpiresIn: 300 # 默认 Captcha 过期时间 300 秒
  Length: 6      # 长度
  CachePrefix: "auth:captcha:" # 设置 Captcha 缓存前缀
